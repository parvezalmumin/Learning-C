 #include <stdio.h>

int main() {
    long long worldPopulation = 8000000000;   // 8 billion
    long long distanceToMoon = 384400000;     // in meters

    printf("World Population: %lld\n", worldPopulation);
    printf("Distance to Moon: %lld meters\n", distanceToMoon);

    return 0;
}
